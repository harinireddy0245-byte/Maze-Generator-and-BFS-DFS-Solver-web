# Maze-Generator-and-BFS-DFS-Solver-web
Maze Solver and Generator is a C++ console application that generates random mazes and solves them using BFS and DFS algorithms. It also includes a rule-based Difficulty Rating system that classifies mazes as Easy, Medium, or Hard based on path length, maze size, and search effort.
